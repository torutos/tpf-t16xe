section .data
    world: ds "Hello World"

section .code
    org 0x0400
    CALL main
    HLT

main:
    MOV BX, world
loop:
    LDB AX, [BX]
    CMP AX, #0
    JEQ done
    STB [0xFFF2], AX
    INC BX
    JMP loop
done:
    RET
